"""Arizona provider-directory mart.

Phase 1 builds identity + CMS staging as plain functions. FastAPI should
import those same functions later rather than re-implementing SQL.
"""

__version__ = "0.1.0"
